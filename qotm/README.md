# Quote of the Moment Service
A simple Flask app that provides a REST endpoint at "/" that returns a random obscure quote as the 'quote' element of a JSON dictionary.

## Interview Scenario
Apply this buggy k8s spec file to a k8s cluster and get this app running in a healthy state.
